# VSCode
.vscode
.classpath
.project
.settings

# User-specific stuff:
.idea/

## File-based project format:
*.iws
*.iml

### Gradle template
.gradle
build/

# Secrets — never commit (kubeconfigs, keystores, secret manifests, env files)
**/.kube/
*.kubeconfig
admin.conf
*.pem
*.p12
*-secret.yaml
values-secret*.yaml
.env

# Vendored Helm subchart packages (re-created by `helm dependency build`)
**/charts/*.tgz

# Deployment solution workspace (kept out of the sample-java-app repo; see SOLUTION.md)
_solution/
